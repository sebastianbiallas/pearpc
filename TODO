- network: "scripts" directory configurable
- intercept focus change and send key-release events
- intercept close window. ask if user really wants to terminate the session
- jitc config
- 2.6 kernel console?
- jmpFixup / Resolve(here) crash?
- mol?
- openbsd pci bla
- dma and who uses it
- PVR testing
- adapt startup logo (name is now PearPC, not PPC)
- locked CDs can't be changed. GUI should reflect this.